/* eslint-disable */

const nombre = "Julian"

  var nombre2 = "Manolo"

  /* eslint-enable */

  const nuevoString = 'Esto es un nuevo string' // eslint-disable-line


  // Hacer que todos los archivos del proyecto sean afectados por las reglas de eslint
  // e incluso arreglar de forma auto

  //nos vamos al package.json a la parte de scripts y luego lo mostrará por terminal los errores
  //    "lint": "eslint .", para buscar errores
  //"lint-fix": "eslint --fix"  para arreglar